import type { LessonOverride } from "./htmlFoundations";

/**
 * Module "Типізація функцій" (ts-functions). Third TypeScript module —
 * builds on ts-basics (annotations, union, narrowing) and ts-objects
 * (interfaces, optional properties). Focus: typing functions themselves —
 * parameters, return values, callbacks, overloads, type predicates, and
 * modeling errors as typed results instead of exceptions. Same beginner
 * rules apply throughout: every term explained before use, compile-time
 * vs runtime distinction kept alive, cheat-sheet lesson shape.
 */
export const tsFunctionsOverrides: Record<string, LessonOverride> = {
  "Типи параметрів функції": {
    interactiveDemo: "parameter-types-demo",
    whatIsIt:
      "Типізація параметрів функції — це анотації типів (детальніше про анотації — у модулі ts-basics), застосовані до кожного параметра функції: function formatOrder(drink: string, size: string = \"medium\", note?: string). Кожен параметр отримує власний тип; параметр може мати значення за замовчуванням (= \"medium\") чи бути необовʼязковим (note?), і кожен із цих варіантів компілятор перевіряє по-своєму.",
    whyUseIt:
      "Функція без типізованих параметрів прийме що завгодно на будь-яке місце — число там, де малось на увазі імʼя напою, чи взагалі пропущений обовʼязковий аргумент. Типізація параметрів робить контракт функції явним: компілятор перевіряє КОЖЕН виклик функції в усьому проєкті проти цього контракту одразу, ще до запуску, — а не лише тоді, коли конкретний неправильний виклик випадково спрацює під час роботи застосунку.",
    whenToUse: [
      "Завжди типізуй параметри функцій — без анотації параметр отримує тип any (\"будь-що\"), і компілятор більше нічого не перевіряє для нього.",
      "Використовуй значення за замовчуванням (= значення) для параметра, який часто має одне й те саме, передбачуване значення.",
      "Використовуй необовʼязковий параметр (?) для параметра, якого реально може не бути в частині викликів.",
    ],
    whenNotToUse: [
      "Не плутай параметр зі значенням за замовчуванням (size: string = \"medium\") із необовʼязковим параметром (note?: string) — перший завжди отримує конкретне значення (передане або замовчування), другий справді може лишитись undefined.",
      "Не став необовʼязковий параметр ПЕРЕД обовʼязковим у списку параметрів — так само, як у звичайному JavaScript, необовʼязкові параметри повинні йти після обовʼязкових.",
      "Не забувай, що тип параметра перевіряється для кожного виклику функції в проєкті — зміна типу параметра може вплинути на код у зовсім інших файлах.",
    ],
    comparisonTable: {
      headers: ["Запис параметра", "Поведінка"],
      rows: [
        ["drink: string", "Обовʼязковий параметр — завжди повинен бути переданий"],
        ["size: string = \"medium\"", "Значення за замовчуванням — якщо не передати, підставляється \"medium\""],
        ["note?: string", "Необовʼязковий параметр — функцію можна викликати без нього, тоді note === undefined"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Функція з трьома типами параметрів одразу:",
        code: `function formatOrder(drink: string, size: string = "medium", note?: string): string {
  return note === undefined
    ? \`\${drink} (\${size})\`
    : \`\${drink} (\${size}), примітка: \${note}\`;
}

formatOrder("Латте", "large", "без цукру");
formatOrder("Латте", "large");
formatOrder("Латте");`,
        lineNotes: [
          "drink: string — обовʼязковий, завжди потрібно передати.",
          "size: string = \"medium\" — якщо не передати другий аргумент, підставиться \"medium\", тип усе одно string.",
          "note?: string — можна не передавати взагалі; тоді всередині функції note === undefined.",
        ],
      },
      {
        before: "Порушення типу параметра:",
        code: `formatOrder("Латте", 42);
// Помилка компілятора:
// Argument of type 'number' is not assignable to parameter of type 'string'.`,
        lineNotes: [
          "42 передається туди, де оголошений тип — string.",
          "Компілятор ловить це одразу, незалежно від того, є в size значення за замовчуванням чи ні — тип перевіряється для будь-якого реально переданого аргументу.",
        ],
      },
    ],
    commonMistakes: [
      "Забувати анотувати параметр і не помічати, що він мовчки отримав тип any.",
      "Плутати необовʼязковий параметр зі значенням за замовчуванням — це різна поведінка щодо undefined.",
      "Ставити необовʼязковий параметр перед обовʼязковим у списку параметрів функції.",
    ],
    dontDoThis: {
      code: `function formatOrder(drink, size = "medium", note) {\n  return \`\${drink} (\${size})\${note ? \`, \${note}\` : ""}\`;\n}\n\nformatOrder(65, "large");`,
      explanation:
        "Без анотацій типів компілятор не бачить жодної проблеми у виклику formatOrder(65, \"large\") — параметр drink мовчки отримав тип any, який приймає геть будь-що, включно з числом там, де малась на увазі назва напою. Це саме той сценарій, де TypeScript без типізації параметрів веде себе так само, як звичайний JavaScript: жодних попереджень до моменту, коли рядок з number всередині шаблонного рядка виведе на екран щось на кшталт \"65 (large)\" замість очікуваної назви напою. Виправлення: додати анотації drink: string, size: string = \"medium\", note?: string — тепер виклик formatOrder(65, \"large\") одразу підкреслюється компілятором. Перевірити можна, додавши ці анотації й спробувавши той самий виклик — помилка зʼявиться миттєво.",
    },
    bestPractices: [
      "Типізуй кожен параметр функції явно — не покладайся на неявний any.",
      "Обирай значення за замовчуванням для параметрів із очевидним, найчастіше потрібним значенням.",
      "Обирай необовʼязковий параметр (?), коли значення реально може бути відсутнім, а не просто \"на випадок чогось\".",
    ],
    remember: [
      "Без анотації параметр отримує тип any — компілятор перестає його перевіряти.",
      "Значення за замовчуванням (= значення) і необовʼязковий параметр (?) — різна поведінка щодо undefined.",
      "Тип параметра перевіряється для кожного виклику функції в усьому проєкті одночасно.",
    ],
    interviewQuestions: [
      { question: "Що станеться з параметром функції, якщо не додати йому анотацію типу?", answer: "Він отримає тип any — компілятор більше не перевірятиме, які значення передають на це місце, і функція втратить частину захисту, який дає типізація." },
      { question: "Чим необовʼязковий параметр (note?: string) відрізняється від параметра зі значенням за замовчуванням (size: string = \"medium\")?", answer: "Необовʼязковий параметр справді може лишитись undefined, якщо його не передали. Параметр зі значенням за замовчуванням завжди отримує конкретне значення — передане явно або підставлене замовчування — і ніколи не буде undefined усередині функції." },
    ],
    summary:
      "Кожен параметр функції варто анотувати явним типом — без анотації параметр стає any і перестає перевірятись. Значення за замовчуванням (= значення) гарантує конкретне значення завжди; необовʼязковий параметр (?) допускає реальну відсутність значення (undefined). Компілятор перевіряє типи параметрів для кожного виклику функції в проєкті одночасно.",
    nextLessonNote: "Далі — типи значень, що повертаються: як типізувати те, що функція віддає назад.",
    practiceTask: {
      title: "Типізуй функцію привітання клієнта",
      description: "Функція формує привітання для клієнта кав'ярні. Додай типи для обовʼязкового імені, необовʼязкового звання постійного клієнта та значення за замовчуванням для мови привітання.",
      checklist: [
        "Параметр name має тип string (обовʼязковий).",
        "Параметр isRegular має тип boolean зі значенням за замовчуванням false.",
        "Параметр title є необовʼязковим типу string.",
      ],
      starterFiles: [
        {
          id: "ts-functions-params-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function greetCustomer(name, isRegular = false, title) {\n  const prefix = title ? \`\${title} \` : "";\n  return isRegular ? \`Раді знову бачити вас, \${prefix}\${name}!\` : \`Вітаємо, \${prefix}\${name}!\`;\n}`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-params-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function greetCustomer(name: string, isRegular: boolean = false, title?: string): string {\n  const prefix = title ? \`\${title} \` : "";\n  return isRegular ? \`Раді знову бачити вас, \${prefix}\${name}!\` : \`Вітаємо, \${prefix}\${name}!\`;\n}`,
          readOnly: true,
        },
      ],
      hints: ["Обовʼязковий параметр не має ні = значення, ні ?."],
      expectedOutput: "Функція типізована повністю: жодного параметра з неявним any.",
    },
    microExercises: [
      { id: "ts-params-predict", kind: "predict", prompt: "function log(message: string, prefix?: string) {} log(\"привіт\"); — чому дорівнює prefix усередині функції?", solution: "undefined. prefix — необовʼязковий параметр, і виклик його не передав." },
      { id: "ts-params-find-bug", kind: "find-the-bug", prompt: "function pay(amount: number, currency?: string = \"UAH\") {} — у чому проблема цього оголошення?", solution: "Не можна одночасно позначити параметр і необовʼязковим (?), і зі значенням за замовчуванням (= \"UAH\") — потрібно обрати щось одне: currency: string = \"UAH\" або currency?: string." },
    ],
  },

  "Типи значень, що повертаються": {
    interactiveDemo: "return-type-demo",
    whatIsIt:
      "Тип значення, що повертається, — це анотація після дужок параметрів функції: function getDeliveryFee(orderTotal: number): number. Вона каже компілятору: ЦЯ функція завжди повинна повернути значення саме такого типу, у кожному можливому шляху виконання — незалежно від того, скільки в ній умов чи гілок.",
    whyUseIt:
      "Функції з кількома гілками (if/else, кілька return) легко випадково залишити один шлях без return чи повернути в ньому значення іншого типу — і ця помилка виявиться лише тоді, коли саме цей рідкісний шлях виконається в реальному застосунку. Явний тип повернення змушує компілятор перевірити КОЖЕН можливий шлях функції одразу, ще до запуску: забута гілка без return чи неправильний тип десь усередині ловляться так само надійно, як і в основному, найчастіше виконуваному шляху.",
    whenToUse: [
      "Функції з кількома гілками виконання (if/else, switch) — тип повернення страхує від забутого return в одній із гілок.",
      "Публічні функції, які використовує інший код, — явний тип повернення документує контракт функції без потреби читати всю її реалізацію.",
      "Коли хочеш, щоб компілятор одразу впіймав помилку, якщо реалізацію функції змінять і випадково зламають тип результату.",
    ],
    whenNotToUse: [
      "Не думай, що тип повернення підставляється автоматично без перевірки, — компілятор справді аналізує кожен шлях виконання функції на відповідність оголошеному типу.",
      "Не забувай про void — спеціальний тип для функцій, які нічого не повертають (детальніше про службові типи — окремо в модулі ts-basics); плутати його зі undefined небажано.",
      "Не покладайся на виведений тип повернення там, де явна анотація зробила б контракт функції зрозумілішим для інших розробників.",
    ],
    comparisonTable: {
      headers: ["Ситуація", "Реакція компілятора"],
      rows: [
        ["Усі гілки повертають number", "Немає помилок"],
        ["Одна з гілок не має return", "Function lacks ending return statement and return type does not include 'undefined'."],
        ["Одна з гілок повертає string замість number", "Type 'string' is not assignable to type 'number'."],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Функція з коректним типом повернення для обох гілок:",
        code: `function getDeliveryFee(orderTotal: number): number {
  if (orderTotal >= 500) {
    return 0;
  }

  return 49;
}`,
        lineNotes: [
          ": number після дужок параметрів — обіцянка, що функція завжди повертає число.",
          "Обидві гілки (if і код після нього) реально повертають number — компілятор задоволений.",
        ],
      },
      {
        before: "Забута гілка без return:",
        code: `function getDeliveryFee(orderTotal: number): number {
  if (orderTotal >= 500) {
    return 0;
  }
  // забули return 49; тут
}
// Помилка компілятора:
// Function lacks ending return statement and return type does not include 'undefined'.`,
        lineNotes: [
          "Якщо orderTotal менше 500, функція нічого не повертає — реально повертається undefined.",
          "Оголошений тип — number, без undefined, тому компілятор одразу вказує на цю прогалину.",
        ],
      },
    ],
    commonMistakes: [
      "Забувати return в одній із гілок розгалуженої функції.",
      "Повертати різні типи в різних гілках, коли оголошений тип повернення — один конкретний тип.",
      "Плутати відсутність return (undefined) із void — це різні речі: void означає, що функція навмисно нічого не повертає, а забутий return у функції з типом number — помилка.",
    ],
    dontDoThis: {
      code: `function getDeliveryFee(orderTotal: number): number {\n  if (orderTotal >= 500) {\n    return 0;\n  }\n  if (orderTotal >= 200) {\n    return "знижена доставка";\n  }\n  return 49;\n}`,
      explanation:
        "Компілятор одразу підкреслює return \"знижена доставка\" червоним. Причина в тому, що оголошений тип повернення — number, а ця гілка повертає рядок, — компілятор перевіряє тип значення в КОЖНІЙ гілці функції окремо, а не лише в останньому чи найпершому return. Без цієї перевірки код, що викликає getDeliveryFee і очікує число для подальших обчислень (наприклад, додавання до суми замовлення), отримав би рядок саме для замовлень від 200 до 499 грн — і арифметика зламалась би саме для цього конкретного діапазону сум, що важко було б одразу помітити. Виправлення: повернути число в цій гілці теж, наприклад return 25;. Перевірити можна, замінивши рядок на число, — помилка компілятора одразу зникає.",
    },
    bestPractices: [
      "Додавай явний тип повернення для будь-якої функції з кількома гілками — це страхує від забутого чи неправильного return.",
      "Тримай усі гілки функції узгодженими: якщо основний шлях повертає number, кожна альтернативна гілка теж повинна повертати number.",
      "Використовуй void для функцій, які навмисно нічого не повертають (наприклад, виводять щось у консоль чи змінюють DOM).",
    ],
    remember: [
      ": Тип після дужок параметрів — анотація типу значення, що повертається.",
      "Компілятор перевіряє тип повернення для КОЖНОЇ гілки функції окремо.",
      "Забутий return у функції з не-void типом повернення — помилка компілятора, а не мовчазний undefined.",
    ],
    interviewQuestions: [
      { question: "Що перевіряє анотація типу повернення функції?", answer: "Що функція реально повертає значення вказаного типу в КОЖНОМУ можливому шляху виконання — компілятор аналізує всі гілки коду (if/else, switch), а не лише один основний шлях." },
      { question: "Що станеться, якщо в розгалуженій функції з типом повернення number забути return в одній із гілок?", answer: "Компілятор покаже помилку: функція фактично поверне undefined у цій гілці, а оголошений тип (number) не включає undefined — тому компіляція не пройде, поки гілку не виправлять." },
    ],
    summary:
      ": Тип після дужок параметрів функції оголошує тип значення, яке функція зобовʼязана повернути в кожному можливому шляху виконання. Компілятор перевіряє всі гілки (if/else, switch) окремо — забутий return чи невідповідний тип в одній із гілок ловиться так само надійно, як і в основному шляху.",
    nextLessonNote: "Далі — типи колбеків: як типізувати функцію, яку передають іншій функції як аргумент.",
    practiceTask: {
      title: "Виправ тип повернення функції знижки",
      description: "Функція розраховує знижку на замовлення, але одна з гілок повертає неправильний тип. Знайди й виправ помилку.",
      checklist: [
        "Функція getDiscount оголошена з поверненням : number.",
        "Кожна гілка функції реально повертає число.",
        "Немає жодної помилки компілятора.",
      ],
      starterFiles: [
        {
          id: "ts-functions-return-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function getDiscount(orderTotal: number): number {\n  if (orderTotal >= 1000) {\n    return 15;\n  }\n  if (orderTotal >= 500) {\n    return "10";\n  }\n  return 0;\n}`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-return-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function getDiscount(orderTotal: number): number {\n  if (orderTotal >= 1000) {\n    return 15;\n  }\n  if (orderTotal >= 500) {\n    return 10;\n  }\n  return 0;\n}`,
          readOnly: true,
        },
      ],
      hints: ["Знайди гілку, де значення повертається в лапках, — це рядок, а не число."],
      expectedOutput: "Усі три гілки функції повертають число, компілятор не показує жодної помилки.",
    },
    microExercises: [
      { id: "ts-return-predict", kind: "predict", prompt: "function double(x: number): number { if (x > 0) return x * 2; } — що покаже компілятор?", solution: "Помилку: Function lacks ending return statement and return type does not include 'undefined'. Гілка, де x не більше нуля, не має return, тож функція реально може повернути undefined, а оголошений тип цього не враховує." },
    ],
  },

  "Типи колбеків": {
    interactiveDemo: "callback-type-demo",
    whatIsIt:
      "Тип колбека — це тип функції, яку передають ІНШІЙ функції як аргумент. Запис (order: number, index: number) => string описує: \"функція, що приймає число й число, а повертає рядок\". Коли параметр іншої функції оголошений саме так — onEach: (order: number, index: number) => string — компілятор очікує, що на це місце передадуть функцію рівно з такою формою.",
    whyUseIt:
      "Колбеки — дуже поширений патерн у JavaScript (array.map, array.filter, обробники подій), і без типізації компілятор не міг би перевірити, чи функція, яку передають, справді приймає правильну кількість аргументів потрібних типів і повертає те, що очікується далі в коді. Тип колбека дозволяє компілятору впіймати помилку прямо там, де колбек написаний, — до того, як array.map чи будь-яка інша функція реально його викличе під час виконання.",
    whenToUse: [
      "Функції, що приймають колбек як аргумент (наприклад, власна обгортка над array.map чи array.filter).",
      "Обробники подій та інші функції, які викликає не сам розробник, а якийсь інший механізм у певний момент.",
      "Будь-яка ситуація, де важливо гарантувати форму (кількість і типи параметрів, тип повернення) функції-аргумента.",
    ],
    whenNotToUse: [
      "Не роби тип колбека надто розпливчастим (наприклад, Function без деталей) — це прибирає всю користь перевірки типів для аргументів і результату колбека.",
      "Не забувай, що колбек перевіряється так само суворо, як і звичайний аргумент, — неправильна кількість чи тип параметрів колбека ловиться компілятором.",
      "Не плутай тип самого колбека з типом значення, яке повертає функція, що ПРИЙМАЄ цей колбек, — це різні анотації.",
    ],
    comparisonTable: {
      headers: ["Оголошення колбека", "Що перевіряє компілятор"],
      rows: [
        ["onEach: (order: number, index: number) => string", "Функція-аргумент повинна приймати number, number і повертати string"],
        ["(price, index) => `#${index}: ${price} грн`", "Дозволено — форма й тип повернення збігаються"],
        ["(price, index) => price * 2", "Помилка — повертає number замість очікуваного string"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Функція, що приймає типізований колбек:",
        code: `function processOrders(
  orders: number[],
  onEach: (order: number, index: number) => string
): string[] {
  return orders.map(onEach);
}

const labels = processOrders([65, 60, 50], (price, index) => \`#\${index}: \${price} грн\`);`,
        lineNotes: [
          "onEach: (order: number, index: number) => string — тип колбека: два числових параметри, повернення — рядок.",
          "Функція, передана у виклику, точно відповідає цій формі — компілятор дозволяє.",
        ],
      },
      {
        before: "Колбек із неправильним типом повернення:",
        code: `processOrders([65, 60, 50], (price, index) => price * 2);
// Помилка компілятора:
// Type 'number' is not assignable to type 'string'.`,
        lineNotes: [
          "Ця функція повертає число, а тип колбека вимагає string.",
          "Компілятор перевіряє тип повернення колбека так само суворо, як і звичайної функції.",
        ],
      },
    ],
    commonMistakes: [
      "Передавати колбек із неправильною кількістю параметрів чи типів параметрів.",
      "Забувати, що тип повернення колбека перевіряється так само, як тип повернення звичайної функції.",
      "Робити тип колбека надто загальним (Function) там, де конкретна форма параметрів і повернення відома заздалегідь.",
    ],
    dontDoThis: {
      code: `function processOrders(\n  orders: number[],\n  onEach: (order: number, index: number) => string\n): string[] {\n  return orders.map(onEach);\n}\n\nprocessOrders([65, 60], (price) => price.toUpperCase());`,
      explanation:
        "Компілятор одразу підкреслює price.toUpperCase() червоним. Причина в тому, що тип колбека оголошує перший параметр як order: number, а .toUpperCase() — метод, що існує лише в string, а не в number. Без цієї перевірки код продовжив би виконуватись до реального виклику .map(onEach) під час виконання, де JavaScript видав би справжню помилку виконання (TypeError: price.toUpperCase is not a function) — уже в робочому застосунку, а не в редакторі коду розробника. Виправлення: використати метод, справді доступний для number, наприклад price.toFixed(2), або обробити число іншим коректним способом. Перевірити можна, замінивши .toUpperCase() на .toFixed(2), — помилка компілятора одразу зникає.",
    },
    bestPractices: [
      "Оголошуй тип колбека максимально конкретно: точна кількість параметрів, їхні типи й тип повернення.",
      "Перевіряй, чи колбек, який передаєш, справді відповідає очікуваній формі, ще до того, як покластись на .map/.filter/подібні виклики.",
      "Використовуй тип колбека для документування контракту функцій вищого порядку — так інші розробники одразу бачать, якої форми функцію очікує параметр.",
    ],
    remember: [
      "(параметри) => тип повернення — це тип функції, яку передають як аргумент (колбек).",
      "Компілятор перевіряє форму й тип повернення колбека так само суворо, як звичайного аргумента.",
      "Типізований колбек ловить помилки на місці виклику, а не під час реального виконання .map/.filter.",
    ],
    interviewQuestions: [
      { question: "Що таке тип колбека?", answer: "Тип функції, яку передають іншій функції як аргумент, — описує кількість і типи параметрів цієї функції-аргумента та тип значення, яке вона повинна повернути." },
      { question: "Чому компілятор ловить помилку, якщо колбек повертає неправильний тип, ще до того, як цей колбек реально викликаний?", answer: "Тому що перевірка типів відбувається на етапі компіляції, аналізуючи саму функцію-колбек та її форму, а не чекаючи фактичного виконання .map чи іншого виклику, який реально запустить колбек під час роботи програми." },
    ],
    summary:
      "Тип колбека ((параметри) => тип повернення) описує форму функції, яку передають іншій функції як аргумент. Компілятор перевіряє передану функцію на відповідність цій формі так само суворо, як і звичайний аргумент, — неправильна кількість параметрів, їхні типи чи тип повернення ловляться ще до реального виконання коду.",
    nextLessonNote: "Далі — перевантаження функцій: як одна функція може мати кілька різних типізованих сигнатур.",
    practiceTask: {
      title: "Типізуй колбек фільтрації замовлень",
      description: "Функція filterOrders приймає масив сум замовлень і колбек-предикат. Додай правильний тип колбека.",
      checklist: [
        "Тип колбека приймає один параметр типу number.",
        "Тип колбека повертає boolean.",
        "Виклик filterOrders із правильним колбеком не викликає помилок.",
      ],
      starterFiles: [
        {
          id: "ts-functions-callback-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function filterOrders(orders: number[], predicate): number[] {\n  return orders.filter(predicate);\n}\n\nfilterOrders([65, 200, 50, 500], (total) => total >= 100);`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-callback-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function filterOrders(orders: number[], predicate: (total: number) => boolean): number[] {\n  return orders.filter(predicate);\n}\n\nfilterOrders([65, 200, 50, 500], (total) => total >= 100);`,
          readOnly: true,
        },
      ],
      hints: ["predicate: (total: number) => boolean — параметр number, повернення boolean."],
      expectedOutput: "filterOrders повертає [200, 500] без жодної помилки компілятора.",
    },
    microExercises: [
      { id: "ts-callback-find-bug", kind: "find-the-bug", prompt: "function run(onDone: (result: string) => void) {} run((result: number) => console.log(result)); — у чому проблема?", solution: "Тип колбека onDone очікує параметр типу string, а передана функція має параметр типу number — типи параметрів колбека не збігаються з оголошеною формою." },
    ],
  },

  "Перевантаження функцій": {
    interactiveDemo: "function-overload-demo",
    whatIsIt:
      "Перевантаження функції (function overload) — це кілька заголовків (сигнатур) з однаковою назвою функції, кожен описує свій набір типів параметрів і повернення, над однією спільною реалізацією. function formatPrice(value: number): string; function formatPrice(value: string): number; — це дві сигнатури: перша каже \"якщо передати число, отримаєш рядок\", друга — \"якщо передати рядок, отримаєш число\". Реалізація функції одна, але компілятор показує розробнику, що викликає formatPrice, лише ту сигнатуру, яка підходить під конкретний тип переданого аргументу.",
    whyUseIt:
      "Деякі функції справді поводяться по-різному залежно від типу вхідних даних: не просто приймають union (number | string) з однаковим типом повернення для обох випадків, а повертають РІЗНІ типи залежно від того, що передали. Без перевантажень довелось би оголосити тип повернення як union (string | number) для будь-якого виклику — і компілятор змушував би перевіряти тип результату щоразу, навіть коли з контексту виклику й так зрозуміло, що для number на вході результат точно буде string. Перевантаження дозволяють компілятору знати точний тип результату вже в момент виклику, спираючись на тип конкретного переданого аргументу.",
    whenToUse: [
      "Коли функція реально повертає різний тип залежно від типу вхідного аргументу (а не завжди той самий union).",
      "Коли хочеш, щоб виклик із конкретним типом аргументу одразу давав точний, а не union-тип результату.",
      "Для невеликої, чітко обмеженої кількості варіантів поведінки — 2-3 сигнатури, не більше.",
    ],
    whenNotToUse: [
      "Не використовуй перевантаження, якщо функція для всіх типів вхідних даних повертає ОДИН і той самий тип результату, — тоді достатньо звичайного union-параметра без перевантажень.",
      "Не роби багато сигнатур \"про запас\" — кожна повинна відповідати реально різній, потрібній поведінці функції.",
      "Не забувай, що спільна реалізація (останній, загальний function-рядок) не видима викликачу — саме сигнатури над нею визначають, які виклики компілятор дозволить.",
    ],
    comparisonTable: {
      headers: ["Підхід", "Тип результату при виклику з number"],
      rows: [
        ["function formatPrice(value: number | string): number | string", "number | string — потрібна додаткова перевірка типу результату"],
        ["Перевантаження: formatPrice(value: number): string; formatPrice(value: string): number;", "string — компілятор точно знає тип результату з типу аргументу"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Дві сигнатури перевантаження над спільною реалізацією:",
        code: `function formatPrice(value: number): string;
function formatPrice(value: string): number;
function formatPrice(value: number | string): number | string {
  if (typeof value === "number") {
    return \`\${value.toFixed(2)} грн\`;
  }
  return Number(value.replace(/[^\\d.]/g, ""));
}`,
        lineNotes: [
          "Перші два рядки — сигнатури перевантаження: вони й описують контракт функції для викликача.",
          "Третій рядок — спільна реалізація, яка приймає union обох типів і реально перевіряє, який саме прийшов, через typeof (детальніше про звуження — у модулі ts-basics).",
        ],
      },
      {
        before: "Виклики з різними типами дають різні типи результату:",
        code: `const asString = formatPrice(65); // тип результату: string, значення "65.00 грн"
const asNumber = formatPrice("65 грн"); // тип результату: number, значення 65`,
        lineNotes: [
          "formatPrice(65) відповідає першій сигнатурі — компілятор знає, що результат буде string.",
          "formatPrice(\"65 грн\") відповідає другій сигнатурі — компілятор знає, що результат буде number.",
        ],
        after: "Кожен виклик отримує точний, а не union-тип результату — залежно від типу переданого аргументу.",
      },
    ],
    commonMistakes: [
      "Створювати перевантаження там, де функція для всіх типів входу повертає той самий тип результату, — тут достатньо звичайного union без перевантажень.",
      "Забувати, що реалізація функції (останній рядок) повинна реально коректно обробляти всі типи, перелічені в сигнатурах над нею.",
      "Робити занадто багато сигнатур перевантаження для функції, поведінку якої простіше описати одним union-типом.",
    ],
    dontDoThis: {
      code: `function formatPrice(value: number): string;\nfunction formatPrice(value: string): number;\nfunction formatPrice(value: number | string): number | string {\n  return String(value);\n}`,
      explanation:
        "Компілятор не зловить це напряму як помилку типів (String(value) технічно завжди повертає string, що сумісно з union number | string), але це порушення обіцянки другої сигнатури: formatPrice(value: string): number обіцяє повернути ЧИСЛО для рядкового входу, а реалізація завжди повертає рядок. Проблема виявиться пізніше: код, що викликає formatPrice(\"65 грн\") і очікує число (наприклад, для подальшої арифметики), реально отримає рядок і зіпсує обчислення — саме тому, що реалізація не відповідає власним заявленим сигнатурам. Виправлення: переконатись, що реалізація дійсно повертає число для рядкового входу (наприклад, через Number(...) після очищення рядка від нецифрових символів), а не просто перетворює будь-що на рядок. Перевірити можна, порівнявши результат виклику з типом, який обіцяє відповідна сигнатура.",
    },
    bestPractices: [
      "Використовуй перевантаження лише тоді, коли тип результату реально залежить від типу вхідних даних — а не для будь-якої функції з union-параметром.",
      "Тримай кількість сигнатур невеликою (2-3) — забагато перевантажень ускладнює читання контракту функції.",
      "Переконуйся, що спільна реалізація реально виконує те, що обіцяє КОЖНА сигнатура над нею, — компілятор не завжди може це перевірити автоматично.",
    ],
    remember: [
      "Перевантаження — кілька сигнатур з однаковою назвою над однією спільною реалізацією.",
      "Кожна сигнатура описує свій набір типів параметрів і повернення для конкретного випадку використання.",
      "Компілятор обирає підхожу сигнатуру за типом переданого аргументу — і показує точний, а не union-тип результату.",
    ],
    interviewQuestions: [
      { question: "Що таке перевантаження функції?", answer: "Кілька заголовків (сигнатур) з однаковою назвою функції, кожен для свого набору типів параметрів і повернення, над однією спільною реалізацією, яка реально обробляє всі перелічені варіанти." },
      { question: "Коли варто використовувати перевантаження замість звичайного union-параметра?", answer: "Коли тип результату функції реально залежить від типу вхідного аргументу — перевантаження дозволяють компілятору знати точний тип результату вже в момент виклику, а не змушувати перевіряти union-тип результату щоразу." },
    ],
    summary:
      "Перевантаження функції — кілька сигнатур з однаковою назвою над спільною реалізацією, кожна описує свій тип параметрів і повернення. Це корисно, коли тип результату реально залежить від типу вхідних даних: компілятор обирає підхожу сигнатуру за типом аргументу й показує точний тип результату замість union.",
    nextLessonNote: "Далі — функції-предикати: спеціальний тип повернення, що дозволяє звужувати типи через виклик функції.",
    practiceTask: {
      title: "Додай перевантаження для парсера кількості",
      description: "Функція parseQuantity повинна повертати число для рядкового входу і рядок (з одиницею виміру) для числового входу. Додай сигнатури перевантаження.",
      checklist: [
        "Сигнатура для string-входу повертає number.",
        "Сигнатура для number-входу повертає string.",
        "Спільна реалізація коректно обробляє обидва випадки.",
      ],
      starterFiles: [
        {
          id: "ts-functions-overload-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `// Додай дві сигнатури перевантаження над цією реалізацією\nfunction parseQuantity(value: number | string): number | string {\n  if (typeof value === "number") {\n    return \`\${value} шт.\`;\n  }\n  return Number(value);\n}`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-overload-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function parseQuantity(value: number): string;\nfunction parseQuantity(value: string): number;\nfunction parseQuantity(value: number | string): number | string {\n  if (typeof value === "number") {\n    return \`\${value} шт.\`;\n  }\n  return Number(value);\n}`,
          readOnly: true,
        },
      ],
      hints: ["Сигнатури пишуться над реалізацією, без тіла функції — лише заголовок і крапка з комою."],
      expectedOutput: "parseQuantity(5) типізується як string, parseQuantity(\"5\") типізується як number.",
    },
    microExercises: [
      { id: "ts-overload-explain", kind: "explain", prompt: "Поясни, чому для функції, яка завжди повертає boolean незалежно від типу вхідного параметра (string | number), перевантаження не потрібні.", solution: "Перевантаження корисні, коли тип РЕЗУЛЬТАТУ залежить від типу вхідних даних. Якщо результат завжди один і той самий тип (boolean) для будь-якого вхідного типу, достатньо звичайного параметра з union-типом (value: string | number): boolean — перевантаження нічого додаткового тут не дають." },
    ],
  },

  "Функції-предикати": {
    interactiveDemo: "type-predicate-demo",
    whatIsIt:
      "Функція-предикат — це функція, тип повернення якої записаний особливим чином: value is Тип замість звичайного boolean. function isValidPrice(value: unknown): value is number технічно повертає true чи false, як і будь-яка перевірка, але цей особливий запис у типі повернення додатково каже компілятору: \"якщо функція повернула true, вважай значення типу number у цьому місці коду\". Це звуження типу (детальніше про звуження — у модулі ts-basics), винесене в окрему, повторно використовувану функцію.",
    whyUseIt:
      "Звичайна перевірка typeof value === \"number\" звужує тип лише всередині одного конкретного if. Якщо та сама перевірка потрібна в кількох місцях коду — чи, що особливо корисно, як аргумент для array.filter() — довелось би повторювати цю перевірку логіку щоразу вручну. Функція-предикат дозволяє винести перевірку один раз, дати їй назву (isValidPrice) і використовувати в будь-якому місці, де потрібне звуження, включно з прямою передачею як колбека у filter — і компілятор коректно звузить тип результату filter, довіряючи предикату.",
    whenToUse: [
      "Коли та сама перевірка типу (чи форми обʼєкта) потрібна в кількох різних місцях коду.",
      "Коли потрібно відфільтрувати масив за типом елементів через array.filter(предикат) і отримати звужений тип результату.",
      "Коли перевірка достатньо складна (кілька умов одразу), щоб виправдати винесення в окрему, іменовану функцію.",
    ],
    whenNotToUse: [
      "Не пиши value is Тип у функції, яка РЕАЛЬНО не перевіряє, чи значення справді цього типу, — компілятор довіряє предикату без додаткової перевірки, тож неправдивий предикат зламає безпеку типів для всього коду, що на нього покладається.",
      "Не використовуй предикат для одноразової перевірки, потрібної рівно в одному місці, — простий typeof чи === null у звичайному if достатній.",
      "Не забувай, що предикат — це звичайна функція з реальною логікою всередині; особливий запис типу повернення нічого не змінює в тому, ЩО функція реально обчислює.",
    ],
    comparisonTable: {
      headers: ["Тип повернення", "Що каже компілятору"],
      rows: [
        ["boolean", "Функція повертає true чи false, тип значення далі не звужується"],
        ["value is number", "Якщо повернуто true, звузь тип значення до number у цьому місці коду"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Функція-предикат, що перевіряє реальну коректність ціни:",
        code: `function isValidPrice(value: unknown): value is number {
  return typeof value === "number" && value > 0;
}

const rawValues: unknown[] = [65, "60", -10, 50, "не число"];
const validPrices = rawValues.filter(isValidPrice);
// Тип validPrices: number[]`,
        lineNotes: [
          "value is number у типі повернення — предикат: звужує тип значення до number, коли функція повертає true.",
          "Усередині функція реально перевіряє typeof value === \"number\" && value > 0 — саме ця реальна перевірка виправдовує обіцянку предиката.",
          "Після .filter(isValidPrice) тип масиву звужується до number[] — компілятор довіряє предикату, і далі можна безпечно виконувати арифметику з елементами.",
        ],
      },
      {
        before: "Без предиката (лише boolean) filter не звужує тип автоматично:",
        code: `function isValidPriceBoolean(value: unknown): boolean {
  return typeof value === "number" && value > 0;
}

const stillUnknown = rawValues.filter(isValidPriceBoolean);
// Тип stillUnknown: unknown[] — компілятор не звужує тип лише через boolean`,
        lineNotes: [
          "Функція технічно робить ТУ САМУ перевірку, але тип повернення — просто boolean, без is.",
          "Тому компілятор не може довести собі, що результат .filter звужений до number[], — тип лишається unknown[].",
        ],
        after: "Саме запис value is number (а не просто boolean) дозволяє компілятору довіряти звуженню після .filter().",
      },
    ],
    commonMistakes: [
      "Писати value is Тип для функції, яка насправді не перевіряє тип значення коректно (наприклад, завжди повертає true).",
      "Плутати звичайний boolean із предикатом — лише is Тип дає компілятору право звужувати тип за результатом функції.",
      "Забувати, що предикат працює і для одиночних if, і як колбек для array.filter() — обидва застосування корисні.",
    ],
    dontDoThis: {
      code: `function isValidPrice(value: unknown): value is number {\n  return true; // "завжди валідно", про всяк випадок\n}\n\nconst prices = [65, "не число", null].filter(isValidPrice);\nconst total = prices.reduce((sum, price) => sum + price, 0);`,
      explanation:
        "Компілятор довіряє предикату isValidPrice беззаперечно: раз функція оголошує value is number, після .filter(isValidPrice) тип масиву звужується до number[], і .reduce() з арифметикою (sum + price) виглядає повністю безпечним для компілятора. Але реальна логіка функції — return true завжди, без жодної справжньої перевірки — тому масив prices насправді містить \"не число\" і null поряд із реальними числами. Під час виконання sum + price для рядка чи null дасть NaN або зіпсований результат, і жодна помилка компілятора цього не попередить, бо предикат ЗАЯВЛЯЄ те, чого реально не перевіряє. Виправлення: повернути реальну перевірку typeof value === \"number\", а не завжди true. Перевірити можна, підставивши реальну перевірку й переконавшись, що масив після filter реально містить лише числа.",
    },
    bestPractices: [
      "Пиши предикат лише тоді, коли функція реально й коректно перевіряє належність значення до заявленого типу.",
      "Використовуй предикати для перевірок, що повторюються в кількох місцях коду чи потрібні як аргумент .filter().",
      "Давай предикату назву, що явно описує, що саме перевіряється (isValidPrice, isNonEmptyString), а не абстрактне check.",
    ],
    remember: [
      "value is Тип у типі повернення — предикат: особливий запис, що дозволяє звужувати тип за результатом виклику функції.",
      "Предикат корисний і в одиночних if, і як колбек для array.filter(), де звужує тип усього масиву.",
      "Компілятор довіряє предикату беззаперечно — реальна перевірка всередині функції повинна відповідати заявленому типу.",
    ],
    interviewQuestions: [
      { question: "Що таке функція-предикат (type predicate)?", answer: "Функція, тип повернення якої записаний як value is Тип замість звичайного boolean — це дозволяє компілятору звужувати тип значення до вказаного типу в місці, де функція повернула true, включно з використанням як колбека для array.filter()." },
      { question: "Чим value is number відрізняється від просто boolean у типі повернення?", answer: "boolean лише повідомляє true чи false, не впливаючи на тип значення далі в коді. value is number додатково каже компілятору звузити тип значення до number, коли функція повернула true, — саме це дозволяє, наприклад, array.filter(предикат) звужувати тип усього масиву результату." },
      { question: "Чому небезпечно писати value is number для функції, яка реально не перевіряє тип значення?", answer: "Компілятор довіряє предикату без додаткової перевірки — якщо функція завжди повертає true незалежно від реального типу значення, увесь код, що покладається на звужений тип після цього предиката, працюватиме з неправильним припущенням про тип даних, і помилки виникнуть лише під час виконання." },
    ],
    summary:
      "Функція-предикат (value is Тип у типі повернення) — це звичайна перевіркова функція з особливим типом повернення, що дозволяє компілятору звужувати тип значення там, де функція повернула true, включно з використанням як колбека для array.filter(). Компілятор довіряє предикату беззаперечно, тож реальна логіка перевірки всередині повинна справді відповідати заявленому типу.",
    proTip: "Якщо та сама перевірка typeof/instanceof/in потрібна більш ніж в одному місці коду — виведи її в іменовану функцію-предикат замість повторення однакового if у кількох файлах.",
    nextLessonNote: "Далі — типи результату з помилкою: як моделювати можливу невдачу функції як звичайне типізоване значення замість винятку.",
    practiceTask: {
      title: "Напиши предикат для валідної назви напою",
      description: "Створи функцію-предикат isNonEmptyString, яка перевіряє, що значення — непорожній рядок, і використай її для фільтрації масиву назв напоїв.",
      checklist: [
        "Функція isNonEmptyString має тип повернення value is string.",
        "Реальна перевірка включає typeof value === \"string\" і value.length > 0.",
        "Результат .filter(isNonEmptyString) має тип string[].",
      ],
      starterFiles: [
        {
          id: "ts-functions-predicate-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `// Напиши функцію-предикат isNonEmptyString(value: unknown): value is string\n\nconst rawNames: unknown[] = ["Латте", "", 42, "Капучино", null];\nconst validNames = rawNames.filter(isNonEmptyString);`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-predicate-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `function isNonEmptyString(value: unknown): value is string {\n  return typeof value === "string" && value.length > 0;\n}\n\nconst rawNames: unknown[] = ["Латте", "", 42, "Капучино", null];\nconst validNames = rawNames.filter(isNonEmptyString);`,
          readOnly: true,
        },
      ],
      hints: ["typeof value === \"string\" перевіряє тип, value.length > 0 перевіряє непорожність."],
      expectedOutput: 'validNames містить ["Латте", "Капучино"] з типом string[].',
    },
    microExercises: [
      { id: "ts-predicate-choice", kind: "choice", prompt: "Яка функція коректно оголошена як предикат для перевірки number?", options: ["function isNum(v: unknown): boolean { return typeof v === \"number\"; }", "function isNum(v: unknown): v is number { return typeof v === \"number\"; }", "function isNum(v: number): v is number { return true; }", "function isNum(v: unknown): number { return typeof v; }"], correctAnswer: "function isNum(v: unknown): v is number { return typeof v === \"number\"; }", solution: "Правильний предикат приймає unknown (бо перевіряє належність до типу, отже вхід заздалегідь невідомого типу), має тип повернення v is number, і реально перевіряє typeof v === \"number\"." },
    ],
  },

  "Типи результату з помилкою": {
    interactiveDemo: "result-type-demo",
    whatIsIt:
      "Result<T> — це поширений шаблон типізації результату функції, яка може або успішно завершитись, або зазнати невдачі: type Result<T> = { ok: true; value: T } | { ok: false; error: string }. Це дискримінована спілка (union із спільним полем-\"перемикачем\" ok, детальніше про звуження через властивість — у модулі ts-objects): якщо ok true, обʼєкт гарантовано має value; якщо ok false — гарантовано має error, і взагалі не має value.",
    whyUseIt:
      "Традиційний спосіб сигналізувати про помилку в JavaScript — кинути виняток (throw) — має суттєвий недолік: сама сигнатура функції ніяк не показує, що вона взагалі МОЖЕ кинути виняток, і легко забути обгорнути виклик у try/catch. Result<T> перетворює можливу невдачу на звичайне, повернене значення: тип функції (Result<number>, а не просто number) уже сам по собі попереджає, що результат треба перевірити, перш ніж використовувати value, — і компілятор змушує це зробити через звуження за полем ok, а не покладається на пам'ять розробника про try/catch.",
    whenToUse: [
      "Функції, що можуть завершитись невдачею передбачувано і часто (ділення на нуль, парсинг рядка, валідація форми) — не як виняткова, рідкісна ситуація.",
      "Коли хочеш, щоб сама сигнатура функції явно показувала можливість невдачі — на відміну від throw, який ніяк не видно в типі.",
      "Коли викликачу функції потрібно обробити і успіх, і невдачу як звичайні, рівноправні випадки, а не одну — через звичайний код, а іншу — через try/catch.",
    ],
    whenNotToUse: [
      "Не використовуй Result<T> для дійсно виняткових, непередбачуваних ситуацій (наприклад, збій мережі на найнижчому рівні) — там традиційний throw і try/catch (детальніше — у курсі JavaScript) залишаються доречними.",
      "Не забувай, що доступ до value чи error поза перевіркою ok — помилка компілятора: forgotten-перевірку ok тут неможливо випадково пропустити так само тихо, як забутий try/catch.",
      "Не плутай Result<T> із Promise — Result описує успіх/невдачу синхронної операції; Promise (детальніше — у курсі JavaScript) описує операцію, що завершиться в майбутньому.",
    ],
    comparisonTable: {
      headers: ["Підхід", "Як дізнатись про можливу невдачу"],
      rows: [
        ["throw new Error(...)", "Ніяк не видно з типу функції — потрібно читати документацію чи реалізацію"],
        ["Result<T> = { ok: true; value: T } | { ok: false; error: string }", "Видно прямо в типі повернення функції; компілятор змушує перевірити ok перед доступом до value"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Функція ділення, що повертає Result<number> замість кидання винятку:",
        code: `type Result<T> = { ok: true; value: T } | { ok: false; error: string };

function divide(a: number, b: number): Result<number> {
  if (b === 0) {
    return { ok: false, error: "Ділення на нуль" };
  }
  return { ok: true, value: a / b };
}`,
        lineNotes: [
          "Result<T> — дискримінована спілка з двома формами: успіх (ok: true, є value) і невдача (ok: false, є error).",
          "divide повертає ЗВИЧАЙНЕ значення в обох випадках — жодного throw.",
        ],
      },
      {
        before: "Обробка результату через звуження за полем ok:",
        code: `const result = divide(10, 0);

if (result.ok) {
  console.log(result.value); // доступний лише тут, де ok === true
} else {
  console.log(result.error); // доступний лише тут, де ok === false
}`,
        lineNotes: [
          "if (result.ok) звужує тип до форми { ok: true; value: number } — .value доступний.",
          "У протилежній гілці тип звужується до { ok: false; error: string } — .error доступний, а .value там узагалі не існує за типом.",
        ],
        after: "Компілятор не дозволить прочитати result.value без попередньої перевірки result.ok — забути цю перевірку неможливо непомітно.",
      },
    ],
    commonMistakes: [
      "Намагатись прочитати .value без попередньої перевірки .ok — компілятор це заборонить, і це варто розуміти як підказку, а не перешкоду.",
      "Плутати Result<T> із Promise — це різні інструменти для різних видів операцій (синхронний результат проти операції в майбутньому).",
      "Використовувати Result<T> для дійсно рідкісних, непередбачуваних збоїв, де традиційний throw / try/catch залишається доречнішим.",
    ],
    dontDoThis: {
      code: `type Result<T> = { ok: true; value: T } | { ok: false; error: string };\n\nfunction divide(a: number, b: number): Result<number> {\n  if (b === 0) {\n    return { ok: false, error: "Ділення на нуль" };\n  }\n  return { ok: true, value: a / b };\n}\n\nconst result = divide(10, 0);\nconsole.log(result.value);`,
      explanation:
        "Компілятор одразу підкреслює result.value червоним. Причина в тому, що result має тип Result<number> — union із двох форм — і без перевірки result.ok компілятор не може знати, у якій саме формі перебуває результат: у формі з value чи у формі з error (де value взагалі не існує). Якби ця перевірка не спрацювала на етапі компіляції, і десь у реальному застосунку divide(10, 0) реально повернув би { ok: false, error: \"Ділення на нуль\" }, result.value дало б undefined — і подальші обчислення з цим undefined зіпсувались би мовчки, без жодного throw, який можна було б перехопити. Виправлення: перевірити result.ok перед доступом до value, обробивши обидва випадки (успіх і невдачу) окремо. Перевірити можна, додавши if (result.ok) { ... } else { ... } — помилка компілятора одразу зникає.",
    },
    bestPractices: [
      "Використовуй Result<T> для передбачуваних, часто виникаючих невдач — валідація, парсинг, ділення, пошук у даних.",
      "Завжди перевіряй поле ok перед доступом до value чи error — компілятор про це нагадає, і цю перевірку варто розуміти як частину контракту функції.",
      "Давай полю error змістовний текст, який реально пояснює причину невдачі, а не загальне \"Помилка\".",
    ],
    remember: [
      "Result<T> = { ok: true; value: T } | { ok: false; error: string } — типізований спосіб моделювати успіх/невдачу без throw.",
      "Перевірка result.ok звужує тип так само, як typeof чи in з попередніх уроків.",
      "Компілятор не дозволить доступ до value без попередньої перевірки ok.",
    ],
    interviewQuestions: [
      { question: "Що таке Result<T> і для чого він потрібен?", answer: "Дискримінована спілка на кшталт { ok: true; value: T } | { ok: false; error: string }, яка моделює можливу невдачу функції як звичайне, типізоване повернене значення замість кидання винятку — сама сигнатура функції явно показує можливість невдачі, а компілятор змушує перевірити результат перед використанням." },
      { question: "Чим Result<T> кращий за throw для передбачуваних помилок?", answer: "throw ніяк не видно в типі функції — легко забути try/catch. Result<T> робить можливу невдачу частиною типу повернення: компілятор не дозволить прочитати .value без попередньої перевірки .ok, тож обробку невдачі неможливо забути непомітно." },
      { question: "Чому компілятор забороняє доступ до result.value одразу після виклику divide(a, b), не перевіривши result.ok?", answer: "result має тип Result<number> — union із двох різних форм обʼєкта. Без перевірки ok компілятор не може знати, у якій саме формі перебуває значення (з value чи з error), тому забороняє доступ до value, доки звуження за полем ok не підтвердить, яка форма реально тут присутня." },
    ],
    summary:
      "Result<T> = { ok: true; value: T } | { ok: false; error: string } моделює можливу невдачу функції як звичайне типізоване значення замість винятку (throw): сама сигнатура функції показує можливість невдачі, а компілятор, через звуження за полем ok, не дозволяє доступ до value без попередньої перевірки результату.",
    nextLessonNote: "Модуль про типізацію функцій завершено. Далі — узагальнені типи (дженерики): як писати функції й типи, що працюють з довільним типом даних, залишаючись типобезпечними.",
    practiceTask: {
      title: "Змодель Result для парсингу кількості товару",
      description: "Створи функцію parseQuantity, яка повертає Result<number>: успіх з розпарсеним числом або невдачу з поясненням, якщо рядок не є коректним числом.",
      checklist: [
        "type Result<T> оголошений як дискримінована спілка з ok: true/false.",
        "Функція повертає { ok: false, error: ... } для некоректного рядка.",
        "Функція повертає { ok: true, value: ... } для коректного числа.",
        "Виклик коду перевіряє result.ok перед доступом до value.",
      ],
      starterFiles: [
        {
          id: "ts-functions-result-start",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `// Опиши type Result<T> і функцію parseQuantity(input: string): Result<number>\n\nfunction parseQuantity(input: string) {\n  const value = Number(input);\n  return value;\n}\n\nconst result = parseQuantity("абв");\nconsole.log(result);`,
        },
      ],
      solutionFiles: [
        {
          id: "ts-functions-result-solution",
          path: "index.ts",
          language: "typescript",
          label: "index.ts",
          code: `type Result<T> = { ok: true; value: T } | { ok: false; error: string };\n\nfunction parseQuantity(input: string): Result<number> {\n  const value = Number(input);\n  if (Number.isNaN(value)) {\n    return { ok: false, error: \`"\${input}" не є числом\` };\n  }\n  return { ok: true, value };\n}\n\nconst result = parseQuantity("абв");\nif (result.ok) {\n  console.log(result.value);\n} else {\n  console.log(result.error);\n}`,
          readOnly: true,
        },
      ],
      hints: ["Number.isNaN(value) реально перевіряє, чи Number(input) дав NaN — саме так виявляється некоректний рядок."],
      expectedOutput: 'Для input = "абв" виводиться повідомлення про помилку через result.error, без падіння програми.',
    },
    microExercises: [
      { id: "ts-result-predict", kind: "predict", prompt: "const result = divide(10, 2); if (result.ok) { console.log(result.value); } — що виведеться?", solution: "5. divide(10, 2) не ділить на нуль, тому повертає { ok: true, value: 5 }, і перевірка result.ok дозволяє доступ до value." },
      { id: "ts-result-find-bug", kind: "find-the-bug", prompt: "const result = divide(10, 0); console.log(result.value ?? result.error); — чому це не найкращий спосіб обробки Result?", solution: "Хоча ?? технічно тут спрацює через те, що value відсутній (undefined) у формі з ok: false, це не використовує реальне звуження типу за полем ok — компілятор не підтвердить безпеку такого доступу до .value напряму без if (result.ok), і для складніших форм Result (де value може бути 0 чи іншим falsy-значенням) такий підхід дасть неправильний результат." },
    ],
  },
};
