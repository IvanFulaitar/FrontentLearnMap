import type { LessonOverride } from "./htmlFoundations";

/**
 * Module "Масиви" (js-arrays). Fourth JavaScript module — creating arrays,
 * accessing elements, and the core transformation methods (map/filter/
 * reduce) used to turn raw data into UI. Same deep cheat-sheet lesson
 * format as js-control-flow and js-functions.
 */
export const jsArraysOverrides: Record<string, LessonOverride> = {
  "Створення масивів і доступ до елементів": {
    whatIsIt: "Масив — впорядкована колекція значень, що створюється літералом [ ]. Кожен елемент має числовий індекс, що починається з 0: перший елемент — arr[0], останній — arr[arr.length - 1]. arr.length завжди дорівнює кількості елементів, а не найбільшому індексу.",
    whyUseIt: "Списки товарів, завдань, коментарів — усе це впорядковані колекції однотипних даних, які природно моделюються масивом і потім рендеряться в інтерфейс. Індексація дозволяє звертатись до конкретної позиції напряму, без пошуку.",
    whenToUse: ["Впорядкована колекція однотипних елементів (товари, завдання, координати).", "Дані, що будуть перетворені чи відрендерені як список в інтерфейсі.", "Позиційний доступ — коли порядок елементів має значення (перший, останній, N-ий)."],
    whenNotToUse: ["Не використовуй масив для колекції з унікальними ключами (ім'я -> значення) — для цього краще обʼєкт чи Map.", "Не покладайся на arr[arr.length] як на \"останній елемент\" — це позиція ЗА межами масиву, останній — arr[arr.length - 1].", "Не створюй масиви з \"дірками\" (sparse arrays, наприклад arr[10] = x без попередніх 0-9) — це ускладнює роботу методів масиву."],
    comparisonTable: {
      headers: ["Вираз", "Що повертає"],
      rows: [
        ["arr[0]", "перший елемент"],
        ["arr[arr.length - 1]", "останній елемент"],
        ["arr[arr.length]", "undefined — позиція ЗА межами масиву"],
        ["arr[-1]", "undefined — JavaScript не підтримує від'ємні індекси як \"з кінця\""],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Створення масиву літералом і базовий доступ за індексом:",
        code: `const products = ["Книга", "Ручка", "Зошит", "Лінійка"];

console.log(products[0]); // "Книга" — перший елемент
console.log(products[2]); // "Зошит"
console.log(products.length); // 4`,
        lineNotes: ["Індексація починається з 0 — products[0] це перший елемент, а не products[1].", "products.length — кількість елементів, обчислюється автоматично, не потрібно рахувати вручну."],
      },
      {
        before: "Звернення за межі масиву не кидає помилку — просто дає undefined:",
        code: `const products = ["Книга", "Ручка", "Зошит", "Лінійка"];

console.log(products[10]); // undefined — такого індексу немає
console.log(products[-1]); // undefined — це НЕ "останній елемент"
console.log(products[products.length - 1]); // "Лінійка" — ось так отримують останній`,
        lineNotes: ["На відміну від деяких інших мов, JavaScript не кидає помилку виходу за межі масиву — тихо повертає undefined.", "products[-1] шукає властивість з ключем \"-1\", якої немає, а не \"елемент з кінця\" — для цього потрібен products.at(-1) чи products[products.length - 1]."],
      },
      {
        before: "Реальний масив обʼєктів — доступ за індексом дає ОБʼЄКТ, властивості якого читаються далі:",
        code: `const cart = [
  { name: "Книга", price: 300 },
  { name: "Ручка", price: 20 },
];

console.log(cart[0]); // { name: "Книга", price: 300 }
console.log(cart[0].name); // "Книга"
console.log(cart[1].price); // 20`,
        lineNotes: ["cart[0] повертає весь обʼєкт на позиції 0 — щоб дістати конкретне поле, додається .name чи .price після індексації.", "Це базовий шаблон для будь-якого списку структурованих даних (товари, користувачі, повідомлення)."],
      },
      {
        before: "Мутація через індекс і додавання елемента через arr[arr.length]:",
        code: `const products = ["Книга", "Ручка"];

products[0] = "Блокнот"; // заміна елемента на позиції 0
products[products.length] = "Зошит"; // додавання в кінець

console.log(products); // ["Блокнот", "Ручка", "Зошит"]`,
        lineNotes: ["products[0] = \"Блокнот\" замінює елемент за вказаним індексом — масив, оголошений через const, можна МУТУВАТИ, хоча саму змінну products перепризначити новим масивом не можна.", "products[products.length] завжди вказує на позицію ОДРАЗУ за останнім елементом — надійний спосіб додати новий елемент в кінець (хоча .push() зазвичай читається чіткіше)."],
        after: "const забороняє products = [...] (нове присвоєння змінній), але НЕ забороняє змінювати вміст масиву — це та сама пастка, що й з обʼєктами.",
      },
    ],
    commonMistakes: ["Використання arr[arr.length] як \"останнього елемента\" замість arr[arr.length - 1] — класична off-by-one помилка.", "Очікування, що arr[-1] дає останній елемент, як у Python — у JavaScript це просто undefined.", "Припущення, що звернення за межі масиву кидає помилку — воно тихо повертає undefined, без жодного попередження.", "Плутанина між const arr (не можна ПЕРЕПРИЗНАЧИТИ змінну) і незмінністю вмісту масиву (вміст МОЖНА мутувати)."],
    dontDoThis: { code: `const products = ["Книга", "Ручка", "Зошит"];\n\nfunction getLastProduct(list) {\n  return list[list.length]; // БАГ: позиція ЗА межами масиву\n}\n\nconsole.log(getLastProduct(products)); // undefined, а не "Зошит"`, explanation: "list[list.length] звертається до позиції ОДРАЗУ ЗА останнім елементом — такого індексу не існує, тому результат undefined. Останній реальний елемент завжди на позиції list.length - 1. Це одна з найпоширеніших off-by-one помилок при роботі з масивами." },
    bestPractices: ["Для останнього елемента завжди використовуй arr[arr.length - 1] чи arr.at(-1).", "Перевіряй arr.length перед припущеннями про наявність елементів на конкретній позиції.", "Не створюй масиви з \"дірками\" — додавай елементи послідовно (.push()) чи через літерал одразу з усім вмістом.", "Пам'ятай, що const arr дозволяє мутувати вміст масиву, забороняючи лише переприсвоєння самої змінної."],
    remember: ["Індексація масиву починається з 0; останній елемент — arr[arr.length - 1].", "arr.length — кількість елементів, а не найбільший допустимий індекс.", "Звернення за межі масиву (чи від'ємний індекс без .at()) дає undefined, а не помилку.", "const забороняє переприсвоєння змінної, але не забороняє мутацію вмісту масиву."],
    interviewQuestions: [
      { question: "З якого індексу починається нумерація елементів масиву в JavaScript?", answer: "З 0. Перший елемент масиву — arr[0], другий — arr[1], і так далі. Останній елемент масиву довжиною N знаходиться на позиції N - 1, тобто arr[arr.length - 1]." },
      { question: "Що поверне arr[arr.length] для масиву з 3 елементів?", answer: "undefined. arr.length дорівнює 3, а arr[3] — це позиція ОДРАЗУ за останнім реальним елементом (індекси 0, 1, 2). Такого елемента не існує, тому JavaScript тихо повертає undefined замість помилки." },
      { question: "Чи можна змінити вміст масиву, оголошеного через const?", answer: "Так. const забороняє лише ПЕРЕПРИЗНАЧЕННЯ самої змінної новим значенням (const arr = [...] знову — заборонено), але дозволяє мутувати вміст того самого масиву: arr[0] = ..., arr.push(...), arr.sort() тощо повністю легальні." },
      { question: "Чому arr[-1] не повертає останній елемент масиву в JavaScript?", answer: "У JavaScript індексація масиву — це, по суті, звернення до властивостей обʼєкта з ключами-рядками \"0\", \"1\", \"2\" тощо. Ключа \"-1\" серед них немає, тому arr[-1] просто повертає undefined, на відміну від мов як Python, де від'ємний індекс означає \"з кінця\". Для цього в JavaScript є метод arr.at(-1)." },
    ],
    summary: "Масив — впорядкована колекція з індексацією від 0; останній елемент — arr[arr.length - 1], а не arr[arr.length] (це вже за межами масиву й дає undefined без помилки). const забороняє переприсвоєння змінної, але дозволяє мутувати вміст масиву.",
    proTip: "Якщо в коді бачиш arr[arr.length] там, де очікується \"останній елемент\" — це майже завжди off-by-one баг. Перевір, чи не пропущено -1.",
    nextLessonNote: "Далі — .map(): як перетворити кожен елемент масиву в новий масив, основа рендеру списків в інтерфейсі.",
    interactiveDemo: "array-access-demo",
    practiceTask: {
      title: "Виправ off-by-one помилку при отриманні останнього товару",
      description: "Функція getLastProduct звертається до products[products.length], що є позицією за межами масиву, тому завжди повертає undefined. Виправ індекс.",
      checklist: ["getLastProduct повертає останній реальний товар зі списку.", "Використано list.length - 1, а не list.length.", "Функція працює для списків будь-якої довжини."],
      starterFiles: [
        {
          id: "js-array-access-start",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  const products = ["Книга", "Ручка", "Зошит"];

  function getLastProduct(list) {
    return list[list.length]; // БАГ: позиція за межами масиву
  }

  document.querySelector("#output").textContent = "Останній товар: " + getLastProduct(products);
  // зараз показує "Останній товар: undefined"
</script>
`,
        },
      ],
      solutionFiles: [
        {
          id: "js-array-access-solution",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  const products = ["Книга", "Ручка", "Зошит"];

  function getLastProduct(list) {
    return list[list.length - 1];
  }

  document.querySelector("#output").textContent = "Останній товар: " + getLastProduct(products);
  // тепер "Останній товар: Зошит"
</script>
`,
          readOnly: true,
        },
      ],
      hints: ["list.length для масиву з 3 елементів дорівнює 3, а індекси — 0, 1, 2.", "Останній елемент завжди на позиції list.length - 1."],
      expectedOutput: "\"Останній товар: Зошит\"",
    },
    microExercises: [
      { id: "js-array-index-predict", kind: "predict", prompt: "Що виведе цей код?", code: `const colors = ["червоний", "синій", "зелений"];\nconsole.log(colors[1]);\nconsole.log(colors.length);`, solution: "\"синій\", потім 3 — colors[1] це другий елемент (індекс з 0), а length — загальна кількість елементів." },
      { id: "js-array-outofrange-find-bug", kind: "find-the-bug", prompt: "У чому проблема цього коду?", code: `const items = ["a", "b", "c"];\nfunction getLast(list) {\n  return list[list.length];\n}\nconsole.log(getLast(items));`, solution: "list[list.length] звертається до позиції ЗА межами масиву (для items довжиною 3 це індекс 3, якого немає) — результат undefined замість очікуваного \"c\". Потрібно list[list.length - 1]." },
      { id: "js-array-length-choice", kind: "choice", prompt: "Для масиву з 5 елементів, який індекс відповідає останньому елементу?", options: ["5", "4", "6", "arr.length"], correctAnswer: "4", solution: "Індексація починається з 0, тому масив з 5 елементів має індекси 0, 1, 2, 3, 4 — останній елемент на позиції 4, тобто length - 1." },
      { id: "js-array-const-mutate-explain", kind: "explain", prompt: "Поясни, чому цей код не кидає помилку: const arr = [1, 2]; arr.push(3);", solution: "const забороняє лише переприсвоєння самої змінної arr новим значенням (arr = [...] знову). push() не перепризначує змінну — він мутує вміст того самого масиву, на який arr продовжує вказувати. Це дозволено навіть для const." },
      { id: "js-array-access-rewrite", kind: "rewrite", prompt: "Перепиши функцію, щоб вона безпечно повертала перший і останній елементи масиву як обʼєкт { first, last }.", code: `function getEdges(list) {\n  return { first: list[0], last: list[list.length] };\n}`, solution: `function getEdges(list) {\n  return { first: list[0], last: list[list.length - 1] };\n}\n// last тепер коректно посилається на останній РЕАЛЬНИЙ елемент` },
    ],
  },

  "Map і рендер списків": {
    whatIsIt: ".map() викликає передану функцію для кожного елемента масиву й повертає НОВИЙ масив тієї ж довжини, заповнений результатами цих викликів. Оригінальний масив залишається незмінним. Це основний спосіб перетворити масив даних у масив React-елементів чи HTML-рядків для рендеру.",
    whyUseIt: "Список товарів, завдань, коментарів рідко відображається \"як є\" — зазвичай кожен елемент потрібно перетворити (додати форматування, обчислити похідне значення, обгорнути в JSX/HTML). .map() робить це декларативно, одним виразом, без ручного for-циклу з тимчасовим масивом.",
    whenToUse: ["Перетворення КОЖНОГО елемента масиву за однаковим правилом (форматування ціни, витягування властивості).", "Рендер списку в інтерфейсі — масив даних -> масив UI-елементів.", "Витягування конкретної властивості з масиву обʼєктів (products.map(p => p.name))."],
    whenNotToUse: ["Не використовуй .map() лише для побічного ефекту (наприклад, console.log кожного елемента) без потреби в новому масиві — для цього призначений .forEach().", "Не використовуй .map(), коли потрібно ВИКИНУТИ деякі елементи — .map() завжди повертає масив тієї ж довжини, для фільтрації потрібен .filter().", "Не мутуй зовнішній стан всередині callback-у .map() — це робить трансформацію нечистою й важчою для розуміння."],
    comparisonTable: {
      headers: ["Метод", "Повертає", "Довжина результату"],
      rows: [
        [".map()", "новий масив результатів", "та сама, що в оригіналі"],
        [".forEach()", "undefined", "не застосовно — не для трансформації"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Базове перетворення чисел через .map():",
        code: `const prices = [100, 250, 400];

const doubled = prices.map((price) => price * 2);

console.log(doubled); // [200, 500, 800]
console.log(prices); // [100, 250, 400] — оригінал не змінився`,
        lineNotes: [".map() створює НОВИЙ масив doubled — prices лишається абсолютно незмінним.", "Callback (price) => price * 2 викликається для КОЖНОГО елемента по черзі, результат кладеться на відповідну позицію нового масиву."],
      },
      {
        before: "Витягування властивості з масиву обʼєктів — поширений реальний шаблон:",
        code: `const products = [
  { name: "Книга", price: 300 },
  { name: "Ручка", price: 20 },
];

const names = products.map((p) => p.name);

console.log(names); // ["Книга", "Ручка"]`,
        lineNotes: ["products.map((p) => p.name) перетворює масив ОБʼЄКТІВ на масив СТРІНГІВ — саме так зазвичай готують дані для випадаючого списку чи простого тексту.", "Кожен p у callback-у — це один обʼєкт з масиву products на відповідній позиції."],
      },
      {
        before: "Побудова HTML-рядків для рендеру списку в DOM:",
        code: `const products = ["Книга", "Ручка", "Зошит"];

const html = products.map((name) => \`<li>\${name}</li>\`).join("");

document.querySelector("#list").innerHTML = html;
// <li>Книга</li><li>Ручка</li><li>Зошит</li>`,
        lineNotes: [".map() перетворює кожну назву товару в рядок <li>...</li>, а .join(\"\") склеює масив рядків у один HTML-рядок без роздільника.", "Це базовий шаблон рендеру списку без фреймворка — у React той самий .map() повертає масив JSX-елементів замість рядків."],
      },
      {
        before: "Забутий return у callback-у .map() — типова помилка, схожа на забутий return у звичайній функції:",
        code: `const prices = [100, 250, 400];

function double(price) {
  const doubled = price * 2; // забутий return!
}

console.log(prices.map(double)); // [undefined, undefined, undefined]`,
        lineNotes: ["double обчислює doubled, але не повертає його — .map() кладе в новий масив те, що callback ФАКТИЧНО повернув, а без return це undefined.", "Масив після .map() завжди тієї ж довжини — тут довжина коректна (3), але кожне значення undefined замість очікуваного числа."],
      },
    ],
    commonMistakes: ["Забутий return у callback-у .map() — масив тієї ж довжини, але заповнений undefined.", "Використання .map() лише для побічного ефекту (без потреби в результаті) — семантично це .forEach().", "Очікування, що .map() може \"пропустити\" елемент — він завжди повертає масив тієї ж довжини, для фільтрації потрібен окремий .filter().", "Мутація елементів оригінального масиву всередині callback-у замість повернення нового значення."],
    dontDoThis: { code: `const prices = [100, 250, 400];\n\nfunction double(price) {\n  const doubled = price * 2; // забутий return\n}\n\nconst result = prices.map(double);\nconsole.log(result); // [undefined, undefined, undefined]`, explanation: "Callback double обчислює doubled, але не повертає це значення — .map() записує в новий масив саме те, що callback реально ПОВЕРНУВ, а це undefined для кожного викликy. Виправлення: додати return doubled; в кінець функції double." },
    bestPractices: ["Використовуй .map() лише коли потрібен новий масив результатів; для побічних ефектів без результату — .forEach().", "Завжди явно повертай значення з callback-у .map() — implicit return для стрілкових функцій без {} тут особливо корисний.", "Для рендеру списків в React додавай key до кожного елемента, отриманого через .map().", "Не мутуй елементи оригінального масиву всередині .map() — повертай нове значення замість зміни existing обʼєкта."],
    remember: [".map() завжди повертає новий масив ТІЄЇ Ж довжини, що оригінал.", "Оригінальний масив після .map() залишається незмінним.", "Забутий return у callback-у дає масив undefined, а не помилку.", "Для побічних ефектів без потреби в новому масиві — .forEach(), не .map()."],
    interviewQuestions: [
      { question: "Що станеться з довжиною масиву після .map()?", answer: "Довжина результату .map() завжди дорівнює довжині оригінального масиву — кожен елемент перетворюється на РІВНО один елемент результату. Якщо потрібно змінити кількість елементів (відкинути частину), .map() не підходить, потрібен .filter()." },
      { question: "Чим .map() відрізняється від .forEach()?", answer: ".map() повертає НОВИЙ масив із результатами callback-у для кожного елемента — призначений для трансформації даних. .forEach() повертає undefined і призначений лише для побічних ефектів (лог, запис у зовнішню змінну) без потреби в новому масиві." },
      { question: "Що поверне prices.map(callback), якщо callback не має return?", answer: "Масив тієї ж довжини, що prices, але кожен елемент буде undefined — .map() кладе в результат саме те, що callback реально повернув, а функція без return завжди повертає undefined, незалежно від обчислень усередині." },
      { question: "Чи мутує .map() оригінальний масив?", answer: "Ні, ЯКЩО сам callback не мутує елементи напряму. .map() створює новий масив і не змінює оригінальний масив чи його елементи-примітиви. Але якщо callback мутує обʼєкт-елемент (наприклад, item.price = ...), цей побічний ефект торкнеться й оригінального масиву, бо обʼєкти передаються за посиланням." },
    ],
    summary: ".map() перетворює кожен елемент масиву за допомогою callback-у й повертає новий масив тієї ж довжини — оригінал лишається незмінним. Забутий return у callback-у дає масив undefined замість очікуваних значень, точно як і в звичайній функції. Для побічних ефектів без потреби в результаті використовується .forEach(), а не .map().",
    proTip: "Якщо після .map() бачиш масив undefined замість очікуваних значень — перше, що варто перевірити: чи не забутий return у callback-функції, особливо для стрілкової функції з {}.",
    nextLessonNote: "Далі — .filter() і .find(): як відібрати частину масиву чи знайти один елемент за умовою.",
    interactiveDemo: "map-render-demo",
    practiceTask: {
      title: "Виправ забутий return у callback-і .map()",
      description: "Функція formatPrice, передана в .map(), обчислює відформатовану ціну, але не повертає її — рендер списку показує \"undefined грн\" для кожного товару. Знайди й виправ помилку.",
      checklist: ["Список коректно показує ціну кожного товару у форматі \"N грн\".", "Функція formatPrice повертає рядок, а не undefined.", "Логіка форматування (додавання \" грн\") лишається без змін."],
      starterFiles: [
        {
          id: "js-map-render-start",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<ul id="list"></ul>

<script>
  const prices = [100, 250, 400];

  function formatPrice(price) {
    const formatted = price + " грн"; // забутий return!
  }

  const html = prices.map(formatPrice).map((text) => "<li>" + text + "</li>").join("");
  document.querySelector("#list").innerHTML = html;
  // зараз показує три пункти "undefined"
</script>
`,
        },
      ],
      solutionFiles: [
        {
          id: "js-map-render-solution",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<ul id="list"></ul>

<script>
  const prices = [100, 250, 400];

  function formatPrice(price) {
    return price + " грн";
  }

  const html = prices.map(formatPrice).map((text) => "<li>" + text + "</li>").join("");
  document.querySelector("#list").innerHTML = html;
  // тепер коректно показує "100 грн", "250 грн", "400 грн"
</script>
`,
          readOnly: true,
        },
      ],
      hints: ["formatPrice обчислює formatted, але не повертає його назовні.", "Додай return formatted; (або return price + \" грн\";) у функцію."],
      expectedOutput: "Список з трьома пунктами: \"100 грн\", \"250 грн\", \"400 грн\".",
    },
    microExercises: [
      { id: "js-map-basic-predict", kind: "predict", prompt: "Що виведе цей код?", code: `const nums = [1, 2, 3];\nconst squared = nums.map((n) => n * n);\nconsole.log(squared);\nconsole.log(nums);`, solution: "[1, 4, 9], потім [1, 2, 3] — .map() створює новий масив squared, а оригінальний nums залишається незмінним." },
      { id: "js-map-noreturn-find-bug", kind: "find-the-bug", prompt: "У чому проблема цього коду?", code: `const names = ["Іван", "Олена"];\nfunction shout(name) {\n  const upper = name.toUpperCase();\n}\nconsole.log(names.map(shout));`, solution: "Функція shout обчислює upper, але не повертає це значення — .map() кладе в результат те, що shout реально повернула (undefined для кожного елемента), тож результат [undefined, undefined] замість [\"ІВАН\", \"ОЛЕНА\"]. Потрібно додати return upper;." },
      { id: "js-map-length-choice", kind: "choice", prompt: "Якою буде довжина результату arr.map(callback) для масиву arr довжиною 5?", options: ["Завжди 5", "Залежить від callback", "Завжди 0", "Може бути будь-якою"], correctAnswer: "Завжди 5", solution: ".map() завжди повертає масив ТІЄЇ Ж довжини, що оригінал — кожен елемент перетворюється рівно на один елемент результату, незалежно від того, що робить callback." },
      { id: "js-map-vs-foreach-explain", kind: "explain", prompt: "Поясни, коли варто використовувати .forEach() замість .map().", solution: ".forEach() варто використовувати, коли потрібен лише побічний ефект для кожного елемента (лог у консоль, оновлення зовнішньої змінної, DOM-маніпуляція) і НЕ потрібен новий масив результатів. .map() семантично неправильний у такому випадку, бо його результат (новий масив) просто ігнорується — це вводить в оману читача коду." },
      { id: "js-map-extract-rewrite", kind: "rewrite", prompt: "Перепиши цикл на .map(), щоб отримати масив лише імен користувачів.", code: `const users = [{ name: "Іван" }, { name: "Олена" }];\nconst names = [];\nfor (let i = 0; i < users.length; i++) {\n  names.push(users[i].name);\n}`, solution: `const users = [{ name: "Іван" }, { name: "Олена" }];\nconst names = users.map((u) => u.name);\n// той самий результат: ["Іван", "Олена"], без ручного циклу й тимчасового масиву` },
    ],
  },

  "Filter і пошук": {
    whatIsIt: ".filter() викликає передану функцію-предикат для кожного елемента й повертає НОВИЙ масив лише з тими елементами, для яких предикат повернув truthy. .find() працює схоже, але повертає ПЕРШИЙ елемент, що пройшов перевірку (не масив), або undefined, якщо жоден не знайдений.",
    whyUseIt: "Пошук по списку, фільтрація за категорією, показ лише активних завдань — усе це відбір підмножини даних за умовою. .filter() дає весь підмножину для відображення списку, .find() — один конкретний елемент за id чи іншим унікальним критерієм.",
    whenToUse: [".filter() — коли потрібен СПИСОК елементів, що відповідають умові (результати пошуку, товари категорії).", ".find() — коли потрібен ОДИН конкретний елемент (за id, унікальним полем) і подальший код очікує сам обʼєкт, а не масив з одним елементом.", ".some()/.every() — коли потрібна лише булева відповідь (чи є бодай один? чи всі відповідають умові?)."],
    whenNotToUse: ["Не пиши arr.filter(cond)[0], коли потрібен один елемент — використовуй arr.find(cond), він зупиняється на першому збігу й читається чіткіше.", "Не забувай, що .filter() завжди повертає масив, навіть порожній — перевіряй .length, а не сам масив на truthy/falsy.", "Не плутай .find() (повертає елемент чи undefined) із .filter()[0] (теж працює, але семантично гірше)."],
    comparisonTable: {
      headers: ["Метод", "Повертає", "Коли зупиняється"],
      rows: [
        [".filter(cond)", "масив (може бути порожнім)", "перевіряє УСІ елементи"],
        [".find(cond)", "один елемент або undefined", "на ПЕРШОМУ збігу"],
        [".some(cond)", "true/false", "на першому true"],
        [".every(cond)", "true/false", "на першому false"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Базовий .filter() з числовим предикатом:",
        code: `const numbers = [3, 8, 15, 4, 22, 1];

const bigNumbers = numbers.filter((n) => n > 10);

console.log(bigNumbers); // [15, 22]
console.log(numbers); // [3, 8, 15, 4, 22, 1] — оригінал незмінний`,
        lineNotes: ["Callback (n) => n > 10 викликається для кожного числа — у результат потрапляють лише ті, для яких він повернув true.", "numbers лишається абсолютно незмінним — .filter(), як і .map(), не мутує оригінал."],
      },
      {
        before: "Фільтрація масиву обʼєктів за властивістю — реальний шаблон \"фільтр за категорією\":",
        code: `const products = [
  { name: "Книга", category: "канцтовари" },
  { name: "Ноутбук", category: "електроніка" },
  { name: "Ручка", category: "канцтовари" },
];

const stationery = products.filter((p) => p.category === "канцтовари");

console.log(stationery.map((p) => p.name)); // ["Книга", "Ручка"]`,
        lineNotes: ["p.category === \"канцтовари\" — предикат перевіряє властивість кожного обʼєкта, а не сам обʼєкт цілком.", "Результат stationery — новий масив ЛИШЕ з обʼєктами, що пройшли перевірку, у тому ж порядку, що в оригіналі."],
      },
      {
        before: ".find() для отримання одного конкретного товару за id:",
        code: `const products = [
  { id: 1, name: "Книга" },
  { id: 2, name: "Ручка" },
  { id: 3, name: "Зошит" },
];

const found = products.find((p) => p.id === 2);

console.log(found); // { id: 2, name: "Ручка" } — сам обʼєкт, не масив
console.log(products.find((p) => p.id === 99)); // undefined — нічого не знайдено`,
        lineNotes: [".find() повертає САМ обʼєкт (чи undefined), а не масив з одним елементом — на відміну від .filter()[0], тут немає проміжного масиву.", "Якщо жоден елемент не проходить перевірку, .find() повертає undefined, а НЕ кидає помилку."],
      },
      {
        before: ".filter()[0] проти .find() — обидва працюють, але з різною ефективністю й читабельністю:",
        code: `const products = [
  { id: 1, name: "Книга" },
  { id: 2, name: "Ручка" },
  { id: 3, name: "Зошит" },
];

const viaFilter = products.filter((p) => p.id === 2)[0]; // перевіряє ВСІ елементи
const viaFind = products.find((p) => p.id === 2); // зупиняється на першому збігу

console.log(viaFilter, viaFind); // однаковий результат: { id: 2, name: "Ручка" }`,
        lineNotes: [".filter() перевіряє КОЖЕН елемент масиву, навіть якщо потрібен лише перший збіг — для великих масивів це зайва робота.", ".find() зупиняється одразу на першому елементі, що відповідає умові, — і семантично чіткіше передає намір \"знайти один\"."],
        after: "Правило: якщо потрібен рівно один елемент — .find(); якщо потрібен весь підмножина для відображення — .filter().",
      },
    ],
    commonMistakes: ["Використання arr.filter(cond)[0] замість arr.find(cond) для пошуку одного елемента.", "Перевірка результату .filter() на truthy/falsy замість перевірки .length — порожній масив [] сам є truthy!", "Забування, що .find() повертає undefined (не помилку), якщо нічого не знайдено — звернення до властивості результату без перевірки кидає TypeError.", "Використання == замість === у предикаті порівняння, що може призвести до неочікуваних збігів через приведення типів."],
    dontDoThis: { code: `const products = [{ id: 1, name: "Книга" }];\n\nconst found = products.find((p) => p.id === 99); // не знайдено -> undefined\n\nconsole.log(found.name); // TypeError: Cannot read properties of undefined`, explanation: ".find() повертає undefined, якщо жоден елемент не відповідає умові — це НЕ помилка сама по собі, це нормальна, очікувана поведінка. Але звернення до found.name без перевірки, чи found дійсно знайдено, кидає справжній TypeError у рантаймі. Правильно: перевіряти if (found) { ... } чи використовувати found?.name перед доступом до властивостей." },
    bestPractices: ["Використовуй .find() замість .filter()[0], коли потрібен рівно один елемент.", "Завжди перевіряй результат .find() на undefined перед зверненням до його властивостей.", "Перевіряй результат .filter() через .length, а не через truthy/falsy самого масиву (порожній масив truthy).", "Використовуй === у предикатах порівняння, щоб уникнути неочікуваних збігів через приведення типів."],
    remember: [".filter() завжди повертає масив (може бути порожнім); .find() повертає елемент або undefined.", ".find() зупиняється на першому збігу — ефективніший за .filter()[0] для пошуку одного елемента.", "Порожній масив [] — truthy, перевіряй .length, а не сам масив.", "Завжди перевіряй результат .find() на undefined перед доступом до властивостей."],
    interviewQuestions: [
      { question: "Чим .find() відрізняється від .filter()?", answer: ".filter() повертає МАСИВ усіх елементів, що відповідають умові (може бути порожнім), і перевіряє кожен елемент масиву. .find() повертає ПЕРШИЙ елемент, що відповідає умові (сам елемент, не масив), або undefined, і зупиняється одразу на першому збігу, не перевіряючи решту." },
      { question: "Чому arr.filter(cond)[0] вважається гіршим стилем, ніж arr.find(cond)?", answer: ".filter() перевіряє УСІ елементи масиву незалежно від того, скільки збігів реально потрібно, а потім [0] бере лише перший результат — це зайва робота для великих масивів. .find() зупиняється одразу на першому збігу й семантично точніше передає намір \"знайти один елемент\"." },
      { question: "Що поверне arr.find(cond), якщо жоден елемент не відповідає умові?", answer: "undefined. Це нормальна, очікувана поведінка, а не помилка. Код, що використовує результат .find(), обов'язково має перевірити його на undefined перед зверненням до властивостей, інакше отримає TypeError у рантаймі." },
      { question: "Чи безпечно перевіряти результат .filter() як if (result) { ... }?", answer: "Ні — порожній масив [] є truthy в JavaScript, тому if (result) буде істинним навіть коли масив порожній і нічого не знайдено. Правильна перевірка — if (result.length > 0) { ... } чи явна перевірка довжини." },
    ],
    summary: ".filter() повертає новий масив з усіма елементами, що відповідають умові (перевіряє всі); .find() повертає ПЕРШИЙ відповідний елемент напряму (не масив) і зупиняється одразу на збігу, або дає undefined. Для пошуку одного елемента .find() ефективніший і чіткіший за .filter()[0]. Порожній масив — truthy, тож для перевірки \"чи щось знайдено\" через .filter() потрібна перевірка .length, а не самого масиву.",
    proTip: "Якщо в код-рев'ю бачиш .filter(cond)[0] — це сигнал запропонувати .find(cond) замість нього: та ж поведінка, ефективніше й читабельніше.",
    nextLessonNote: "Далі — .reduce(): найпотужніший метод масиву, для підрахунку сум, підсумків і побудови нових структур даних.",
    interactiveDemo: "filter-search-demo",
    practiceTask: {
      title: "Виправ пошук товару, що завжди повертає undefined",
      description: "Пошук товару за назвою порівнює рядки без урахування регістру некоректно (== замість перевірки в нижньому регістрі), тому пошук \"ручка\" (з малої літери) не знаходить товар \"Ручка\". Виправ порівняння.",
      checklist: ["Пошук \"ручка\" (малими) знаходить товар \"Ручка\".", "Пошук нечутливий до регістру символів.", "Логіка пошуку використовує .find(), а не .filter()[0]."],
      starterFiles: [
        {
          id: "js-filter-search-start",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  const products = [{ name: "Ручка" }, { name: "Книга" }];
  const query = "ручка";

  const found = products.find((p) => p.name === query); // порівняння чутливе до регістру

  document.querySelector("#output").textContent = found ? found.name : "не знайдено";
  // зараз показує "не знайдено"
</script>
`,
        },
      ],
      solutionFiles: [
        {
          id: "js-filter-search-solution",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  const products = [{ name: "Ручка" }, { name: "Книга" }];
  const query = "ручка";

  const found = products.find((p) => p.name.toLowerCase() === query.toLowerCase());

  document.querySelector("#output").textContent = found ? found.name : "не знайдено";
  // тепер показує "Ручка"
</script>
`,
          readOnly: true,
        },
      ],
      hints: ["p.name === query порівнює рядки точно, з урахуванням регістру.", "Приведи обидва рядки до нижнього регістру перед порівнянням: .toLowerCase()."],
      expectedOutput: "\"Ручка\"",
    },
    microExercises: [
      { id: "js-filter-basic-predict", kind: "predict", prompt: "Що виведе цей код?", code: `const nums = [5, 12, 8, 20];\nconsole.log(nums.filter((n) => n > 10));`, solution: "[12, 20] — .filter() повертає новий масив лише з елементами, що пройшли перевірку n > 10." },
      { id: "js-find-vs-filter-find-bug", kind: "find-the-bug", prompt: "У чому проблема цього коду?", code: `const users = [{ id: 1, name: "Іван" }];\nconst user = users.find((u) => u.id === 2);\nconsole.log(user.name);`, solution: "users.find((u) => u.id === 2) не знаходить жодного користувача з id 2, тому повертає undefined. Звернення до user.name без перевірки кидає TypeError: Cannot read properties of undefined. Потрібна перевірка if (user) перед доступом до .name." },
      { id: "js-empty-array-truthy-choice", kind: "choice", prompt: "Чи є порожній масив [] truthy чи falsy в умові if?", options: ["Truthy", "Falsy", "Залежить від типу елементів", "Кидає помилку"], correctAnswer: "Truthy", solution: "Порожній масив [] — це обʼєкт, а всі обʼєкти в JavaScript truthy, незалежно від вмісту. Перевірка \"чи щось знайдено\" через .filter() має йти через .length > 0, а не через сам масив." },
      { id: "js-find-vs-filter-explain", kind: "explain", prompt: "Поясни, чому .find() ефективніший за .filter(cond)[0] для великого масиву.", solution: ".filter() завжди перевіряє КОЖЕН елемент масиву від початку до кінця, будуючи повний масив результатів, і лише потім [0] бере перший. .find() зупиняється одразу, щойно знаходить перший елемент, що відповідає умові, — для великого масиву з збігом на початку це може бути суттєво швидше." },
      { id: "js-filter-to-find-rewrite", kind: "rewrite", prompt: "Перепиши пошук користувача за id з .filter()[0] на .find().", code: `const users = [{ id: 1 }, { id: 2 }];\nconst user = users.filter((u) => u.id === 2)[0];`, solution: `const users = [{ id: 1 }, { id: 2 }];\nconst user = users.find((u) => u.id === 2);\n// той самий результат, без проміжного масиву й зупинкою на першому збігу` },
    ],
  },

  "Reduce для підсумків": {
    whatIsIt: ".reduce() \"згортає\" масив в ОДНЕ значення, викликаючи callback для кожного елемента з поточним акумулятором. Синтаксис: arr.reduce((acc, item) => новеAcc, початковеЗначення). Початкове значення — старт акумулятора; без нього reduce бере ПЕРШИЙ елемент масиву як старт, що працює ненадійно.",
    whyUseIt: "Сума кошика, підрахунок кількості, побудова обʼєкта-довідника з масиву — усе це \"згортання\" списку в одне підсумкове значення. .reduce() — найзагальніший метод масиву: через нього технічно можна реалізувати навіть .map() і .filter().",
    whenToUse: ["Підрахунок суми, кількості, середнього значення з масиву.", "Групування масиву в обʼєкт за ключем (наприклад, товари за категорією).", "Пошук мінімуму/максимуму, коли потрібна логіка складніша за готові Math.min/Math.max."],
    whenNotToUse: ["Не використовуй .reduce() без початкового значення на масиві, що МОЖЕ бути порожнім, — це кидає TypeError.", "Не використовуй .reduce() там, де є простіший спеціалізований метод (Math.max(...arr) замість reduce для максимуму) — читабельність важливіша.", "Не будуй надто складну логіку в одному callback-і reduce — якщо важко зрозуміти з першого погляду, розбий на кілька кроків."],
    comparisonTable: {
      headers: ["Виклик", "Поведінка на порожньому масиві"],
      rows: [
        ["arr.reduce(fn, 0)", "повертає 0 (початкове значення) — безпечно"],
        ["arr.reduce(fn)", "кидає TypeError: Reduce of empty array with no initial value"],
      ],
    },
    codeWalkthroughs: [
      {
        before: "Базова сума чисел з явним початковим значенням:",
        code: `const numbers = [10, 20, 30];

const total = numbers.reduce((sum, n) => sum + n, 0);

console.log(total); // 60`,
        lineNotes: ["0 — початкове значення акумулятора sum перед першою ітерацією.", "Кожна ітерація: sum = sum + n, результат передається в наступну ітерацію як новий sum."],
      },
      {
        before: "Реальний розрахунок суми кошика з масиву обʼєктів:",
        code: `const cart = [
  { name: "Книга", price: 300, quantity: 2 },
  { name: "Ручка", price: 20, quantity: 5 },
];

const total = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);

console.log(total); // 700`,
        lineNotes: ["Кожна ітерація додає price * quantity ОДНОГО товару до накопиченої суми sum.", "0 гарантує коректний старт незалежно від того, скільки товарів у кошику — навіть якщо cart порожній, результат буде 0, а не помилка."],
      },
      {
        before: "Без початкового значення reduce на порожньому масиві кидає справжню помилку:",
        code: `const emptyCart = [];

try {
  const total = emptyCart.reduce((sum, item) => sum + item.price);
} catch (err) {
  console.log(err.message); // "Reduce of empty array with no initial value"
}`,
        lineNotes: ["Без другого аргументу reduce намагається взяти ПЕРШИЙ елемент масиву як початковий акумулятор — якщо масив порожній, такого елемента немає.", "Це не гіпотетична, а справжня помилка, яку JavaScript кидає в реальному часі виконання — саме тому початкове значення важливе навіть \"на всякий випадок\"."],
        after: "Правило: завжди передавай початкове значення в reduce, особливо якщо масив теоретично може бути порожнім.",
      },
      {
        before: "Групування масиву в обʼєкт-довідник за категорією — reduce будує НОВУ структуру даних, не лише число:",
        code: `const products = [
  { name: "Книга", category: "канцтовари" },
  { name: "Ноутбук", category: "електроніка" },
  { name: "Ручка", category: "канцтовари" },
];

const byCategory = products.reduce((groups, p) => {
  if (!groups[p.category]) groups[p.category] = [];
  groups[p.category].push(p.name);
  return groups;
}, {});

console.log(byCategory);
// { "канцтовари": ["Книга", "Ручка"], "електроніка": ["Ноутбук"] }`,
        lineNotes: ["Початкове значення тут — порожній обʼєкт {}, а не 0 — акумулятор не завжди число.", "Кожна ітерація перевіряє, чи вже є масив для цієї категорії, і додає туди назву товару — в кінці groups повертається як новий акумулятор для наступної ітерації."],
      },
    ],
    commonMistakes: ["Виклик .reduce() без початкового значення на масиві, що може бути порожнім — TypeError у рантаймі.", "Забування, що callback має ЯВНО повернути новий акумулятор на кожній ітерації (return groups; у прикладі вище) — без return акумулятор стає undefined.", "Використання .reduce() для простих задач, де .map()/.filter() чи спеціалізований метод (Math.max) читались би чіткіше.", "Плутанина порядку аргументів callback-у: (акумулятор, елемент), а не (елемент, акумулятор)."],
    dontDoThis: { code: `function getCartTotal(cart) {\n  return cart.reduce((sum, item) => sum + item.price * item.quantity);\n  // без початкового значення!\n}\n\nconsole.log(getCartTotal([])); // TypeError: Reduce of empty array with no initial value`, explanation: "Без другого аргументу (початкового значення) reduce намагається взяти ПЕРШИЙ елемент масиву як стартовий акумулятор. Для порожнього кошика такого елемента немає, і JavaScript кидає справжній TypeError замість повернення 0. Це особливо небезпечно, бо код може \"працювати\" місяцями в production, доки хтось не викличе функцію з порожнім масивом. Виправлення: додати , 0 як третій аргумент reduce." },
    bestPractices: ["Завжди передавай початкове значення в .reduce(), навіть коли масив \"здається\" завжди непорожнім.", "Явно повертай акумулятор з кожної гілки callback-у — легко забути return, коли логіка складніша за просту суму.", "Для простих задач (максимум, мінімум) розглянь спеціалізовані методи перед reduce — читабельність важливіша за \"елегантність\".", "Обирай тип початкового значення відповідно до задачі: 0 для сум, [] для побудови масиву, {} для побудови обʼєкта."],
    remember: [".reduce() згортає масив в одне значення через callback(акумулятор, елемент).", "Без початкового значення .reduce() кидає TypeError на порожньому масиві.", "Початкове значення визначає тип результату: 0 для чисел, [] для масивів, {} для обʼєктів.", "Callback має ЯВНО повернути новий акумулятор на кожній ітерації."],
    interviewQuestions: [
      { question: "Що станеться при виклику [].reduce((a, b) => a + b) без початкового значення?", answer: "JavaScript кидає TypeError: Reduce of empty array with no initial value. Без початкового значення reduce намагається взяти перший елемент масиву як стартовий акумулятор, а для порожнього масиву такого елемента немає." },
      { question: "Які два аргументи приймає callback у .reduce() і в якому порядку?", answer: "Перший аргумент — акумулятор (накопичене значення з попередніх ітерацій, чи початкове значення на першій ітерації), другий — поточний елемент масиву. Порядок важливий: (acc, item), а не (item, acc)." },
      { question: "Чому важливо явно повертати акумулятор з кожної гілки callback-у reduce, навіть у складній логіці з if?", answer: "Значення, яке callback повертає на кожній ітерації, СТАЄ акумулятором для наступної ітерації. Якщо якась гілка коду (наприклад, всередині if) не має явного return, JavaScript використає undefined як акумулятор далі, і вся подальша обробка зламається." },
      { question: "Чи можна реалізувати .map() через .reduce()?", answer: "Так — .reduce() є найзагальнішим методом масиву: arr.reduce((acc, item) => [...acc, transform(item)], []) дає той самий результат, що arr.map(transform). На практиці .map() читається чіткіше для простої трансформації, а .reduce() використовують, коли потрібна складніша логіка накопичення." },
    ],
    summary: ".reduce() згортає масив в одне підсумкове значення через callback(акумулятор, елемент), повертаючи новий акумулятор на кожній ітерації. Без початкового значення reduce бере перший елемент масиву як старт — і кидає справжній TypeError на порожньому масиві. Початкове значення визначає тип результату: число для сум, масив чи обʼєкт для складніших структур.",
    proTip: "Завжди передавай явне початкове значення в .reduce() — навіть коли \"впевнений\", що масив ніколи не буде порожнім. Цей аргумент коштує один символ (,0), а рятує від рантайм-помилки в production.",
    nextLessonNote: "Модуль \"Масиви\" триває — далі сортування даних: як .sort() змінює масив і чому це може бути неочікувано.",
    interactiveDemo: "reduce-totals-demo",
    practiceTask: {
      title: "Виправ TypeError через відсутнє початкове значення reduce",
      description: "Функція getCartTotal рахує суму кошика через .reduce() без початкового значення, тому кидає помилку на порожньому кошику. Виправ, додавши початкове значення 0.",
      checklist: ["getCartTotal([]) повертає 0, а не кидає помилку.", "getCartTotal з товарами повертає коректну суму (price * quantity кожного).", "Використано явне початкове значення 0."],
      starterFiles: [
        {
          id: "js-reduce-totals-start",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  function getCartTotal(cart) {
    return cart.reduce((sum, item) => sum + item.price * item.quantity);
    // без початкового значення!
  }

  try {
    const result = getCartTotal([]);
    document.querySelector("#output").textContent = "Сума: " + result;
  } catch (err) {
    document.querySelector("#output").textContent = "Помилка: " + err.message;
  }
  // зараз показує "Помилка: Reduce of empty array with no initial value"
</script>
`,
        },
      ],
      solutionFiles: [
        {
          id: "js-reduce-totals-solution",
          path: "index.html",
          language: "html",
          label: "index.html",
          code: `<p id="output"></p>

<script>
  function getCartTotal(cart) {
    return cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  }

  try {
    const result = getCartTotal([]);
    document.querySelector("#output").textContent = "Сума: " + result;
  } catch (err) {
    document.querySelector("#output").textContent = "Помилка: " + err.message;
  }
  // тепер показує "Сума: 0"
</script>
`,
          readOnly: true,
        },
      ],
      hints: ["Без другого аргументу reduce бере перший елемент масиву як старт — на порожньому масиві такого елемента немає.", "Додай , 0 як другий аргумент reduce."],
      expectedOutput: "\"Сума: 0\"",
    },
    microExercises: [
      { id: "js-reduce-sum-predict", kind: "predict", prompt: "Що виведе цей код?", code: `const nums = [2, 4, 6];\nconsole.log(nums.reduce((sum, n) => sum + n, 0));`, solution: "12 — акумулятор sum починається з 0 і накопичує суму всіх елементів: 0+2=2, 2+4=6, 6+6=12." },
      { id: "js-reduce-empty-find-bug", kind: "find-the-bug", prompt: "У чому проблема цього коду?", code: `function getTotal(items) {\n  return items.reduce((sum, item) => sum + item.price);\n}\nconsole.log(getTotal([]));`, solution: "Без початкового значення reduce намагається взяти перший елемент масиву як старт — для порожнього масиву items такого елемента немає, і JavaScript кидає TypeError: Reduce of empty array with no initial value. Потрібно додати , 0 як другий аргумент reduce." },
      { id: "js-reduce-initial-type-choice", kind: "choice", prompt: "Яке початкове значення варто передати в reduce, щоб згрупувати масив товарів в обʼєкт за категорією?", options: ["0", "[]", "{}", "null"], correctAnswer: "{}", solution: "Для побудови обʼєкта-довідника (групування за ключем) акумулятор має бути обʼєктом з самого початку — {}. 0 підходить для сум чисел, [] — для побудови нового масиву." },
      { id: "js-reduce-return-explain", kind: "explain", prompt: "Поясни, чому важливо явно повертати акумулятор з callback-у reduce на кожній ітерації.", solution: "Значення, яке callback повертає на поточній ітерації, стає акумулятором для НАСТУПНОЇ ітерації. Якщо callback (наприклад, через забутий return усередині складнішої логіки з if) не повертає нове значення явно, наступна ітерація отримає undefined замість очікуваного акумулятора, і вся подальша обробка зламається." },
      { id: "js-reduce-count-rewrite", kind: "rewrite", prompt: "Перепиши підрахунок кількості товарів певної категорії через .reduce() замість .filter().length.", code: `const products = [{ category: "книги" }, { category: "іграшки" }, { category: "книги" }];\nconst count = products.filter((p) => p.category === "книги").length;`, solution: `const products = [{ category: "книги" }, { category: "іграшки" }, { category: "книги" }];\nconst count = products.reduce((total, p) => p.category === "книги" ? total + 1 : total, 0);\n// той самий результат (2), без побудови проміжного масиву через filter` },
    ],
  },
};
